---
interface Props {
  title: string;
  subtitle: string;
}
const { title, subtitle } = Astro.props;
---

<div class="grid max-w-screen-xl mt-6 mx-auto gap-14 lg:grid-cols-12">
  <div class="hidden lg:mt-0 lg:col-span-6 lg:flex max-h-80">
    <slot name="picture" />
  </div>
  <div class="mr-auto lg:col-span-6 text-center md:text-left content-center">
    <h2
      class="max-w-2xl mb-4 text-2xl font-bold tracking-tight leading-none md:text-5xl xl:text-6x">
      {title}
    </h2>
    <p
      class="max-w-2xl mb-6 font-light text-gray-500 lg:mb-8 md:text-lg lg:text-xl">
      {subtitle}
    </p>
  </div>
</div>
