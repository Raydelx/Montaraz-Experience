---
import { Picture } from "astro:assets";
import heroImage from "../assets/hero-cycling.jpg";
import Link from "../components/ui/link.astro";
import { Icon } from "astro-icon";
---

<main
  class="grid lg:grid-cols-2 place-items-center gap-10 p-5 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 md:block">
    <Picture
      src={heroImage}
      alt="Astronaut in the air"
      widths={[200, 400, 600]}
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
      class="rounded-xl"
    />
  </div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      The best tours, just for you and your family
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      Montaraz Experience has as a premise the well-being of our clients, as
      well as their happiness and joy. In addition to a responsible tourism with
      the environment.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="#"
        href="/"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="bx:bxs-cloud-download" />

        Free Guide
      </Link>
      <Link
        size="lg"
        style="muted"
        rel="noopener"
        href="/"
        class="flex gap-1 items-center justify-center">
        <Icon class="text-black w-5 h-5" name="bx:bxl-github" />
        Socials
      </Link>
    </div>
  </div>
</main>
